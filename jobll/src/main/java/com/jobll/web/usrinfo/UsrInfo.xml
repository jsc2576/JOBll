<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jobll.web.usrinfo.UsrInfo">

	<select id="select" resultType="com.jobll.web.usrinfo.UsrInfo">
		SELECT * from usr_info
	</select>
	
	<select id="checkId" parameterType="java.lang.String" resultType="Integer">
		select count(*) 
		from usr_info
		where usr_id = #{usr_id}
	</select>
	
	<insert  id="create" parameterType ="com.jobll.web.usrinfo.UsrInfo">
	INSERT INTO usr_info(
          	usr_id,
			usr_nm,
			usr_lv,
			tel_num,
			mobile_num,
			usr_email,
			usr_pwd,
			reg_date,
			act_stus,
			usr_cmpny_nm
	)
    VALUES (
			#{usr_id},
			#{usr_nm},
			#{usr_lv},
			#{tel_num},
			#{mobile_num},
			#{usr_email},
			#{usr_pwd},
			#{reg_date},
			#{act_stus},
			#{usr_cmpny_nm}
	)
	</insert>

</mapper>