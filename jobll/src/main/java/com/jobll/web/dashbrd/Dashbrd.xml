<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jobll.web.dashbrd">

	<select id="findCuntPrjtSbjt" parameterType="com.jobll.web.dashbrd.Dashbrd" resultType="hashmap">
		SELECT prjt_sbjt
		FROM prjt_info p
			INNER JOIN hsty_info h ON p.prjt_idx = h.prjt_idx
		WHERE p.usr_id = #{usr_id} 
	</select>
	
	<select id="findCnetPrjtSbjt" parameterType="com.jobll.web.dashbrd.Dashbrd" resultType="hashmap">
		SELECT prjt_sbjt
		FROM prjt_info p
			INNER JOIN prjt_sup_list psl ON p.prjt_idx = psl.prjt_idx
		WHERE p.usr_id = #{usr_id} 
	</select>
</mapper>
